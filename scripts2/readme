Injecting your script into a host.

Variants on host lifecyle are 1)="fireup" or use 2)=existing:

1) kvm_factory.sh create host specified ny os-type and hostname
2) static_factory.sh connect to existing hosts

